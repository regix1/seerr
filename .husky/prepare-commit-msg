#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Only run Commitizen interactively when a TTY is available (skip in GUIs)
if [ -t 0 ] && [ -e /dev/tty ]; then
  exec < /dev/tty && npx cz --hook || true
fi
